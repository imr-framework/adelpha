[project]
name = "adelpha"
version = "0.1.0"
description = "Adelpha — Intelligent Magnetic Resonance Framework (Tauri/React GUI + Python supervisor)."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
docs = [
    "zensical>=0.0.50",
]

[tool.uv]
package = false
