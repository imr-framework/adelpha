# Runtime-only Python dependencies for the Adelpha supervisor sidecar.
# DTAM (numpy 2.x stack) is installed from dtam/pyproject.toml when present.
# The heavy MRI4ALL console stack (numpy 1.25, numba, PyQt5) is intentionally
# not installed — the HTTP façade uses the sequence FALLBACK catalog if the
# full SequenceBase registry cannot import.

fastapi>=0.115.0
httpx>=0.28.1
pydantic>=2.2
pydicom>=1.2.2
uvicorn[standard]>=0.32.0
websockets>=12.0
pyyaml>=6.0
numpy>=2.2.6
